int main() {
char str[100];
    int i;
    gets(str);

    if(strlen(str)<5&&strlen(str)>100)
        printf("YuKK!!");
    else if{
        for(i=65;i<=90;i++)
            {
            if(strchr(str,i))
        }
        for(i=97;i<=122;i++)
            {if(strchr(str,i))
            }
        for(i=48;i<=57;i++)
            {
            if(strchr(str,i))
        }
        for(i=33;i<=38;i++)
        {if(strchr(str,i))

        }
        printf("YaHoo!!");
        else
            printf("Yukk!!");
    }
